<?xml version="1.0" encoding="utf-8"?><!--
   Sample backup rules file; uncomment and customize as necessary.
   See https://developer.android.com/guide/topics/data/autobackup
   for details.
   Note: This file is ignored for devices older than API 31
   See https://developer.android.com/about/versions/12/backup-restore
-->
<full-backup-content>
    <!-- Gemini/OpenAI/DeepSeek/Claude API keys live here (SettingsRepository) — never include in
         backups. GOALS.md §18e: path changed from "datastore/settings.preferences_pb" (Android's
         old Context.dataStore delegate convention) to "settings.preferences_pb" directly under
         filesDir, since the KMP-safe DataStore setup writes straight to a file path we control. -->
    <exclude domain="file" path="settings.preferences_pb"/>
</full-backup-content>